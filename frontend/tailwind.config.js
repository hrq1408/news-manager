/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./index.html",
      "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
      extend: {}
    },
    plugins: [],
  }
  
  
  // trocar as cores

  // black blue
  // rgb(5 12 22)

  // blue
  // rgb(48 123 230)

  // light gray
  // rgb(97 119 144)

  // segunda página com o id da matéria (react-router)
  // pagina de admin com os CRUDs
  // criar controle de estado para o admin

  // criar o banco de dados para trazer as matérias

  // testes unitários dos módlos principais

  // garantir o MVC no backend